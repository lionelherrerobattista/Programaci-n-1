
/** \brief Toma un dato int de la estructura que se le pasa a la función
 *
 * \param pEstructura void* estructura que se le pasa a la función
 * \return int devuelve el dato int almacenado en la estructura
 *
 */
int getNumber(void* pEstructura);


/** \brief Toma un dato char* de la estructura que se le pasa a la función
 *
 * \param pEstructura void* estructura que se le pasa a la función
 * \return char* devuelve el dato char* almacenado en la estructura
 *
 */
char* getString(void* pEstructura);
